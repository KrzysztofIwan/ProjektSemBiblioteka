<Window x:Class="ProjektSemBiblioteka.Views.UzytkownicyDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjektSemBiblioteka.Views"
        mc:Ignorable="d"
        Title="Aplikacja WPF - Użytkownicy" Height="640" Width="1200" FontSize="20" FontWeight="Medium" ResizeMode="NoResize"
            WindowStartupLocation="CenterScreen">
    <Grid>
        <ListView ItemsSource="{Binding UzytkownicyList}" Background="#c1e1ff">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Kategoria" DisplayMemberBinding="{Binding Imie}"/>
                        <GridViewColumn Header="Tytul" DisplayMemberBinding="{Binding Nazwisko}"/>
                        <GridViewColumn Header="Id_Autor" DisplayMemberBinding="{Binding Adres}"/>
                        <GridViewColumn Header="Id_Wydawnictwo" DisplayMemberBinding="{Binding Nr_Telefonu}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
